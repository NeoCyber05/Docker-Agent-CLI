[project]
name = "docker-agent"
version = "0.1.0"
description = "Natural-language CLI for managing Docker infrastructure via an LLM agent. Python rewrite of the TypeScript docker-agent CLI."
readme = "README.md"
requires-python = ">=3.11"
authors = [{ name = "docker-agent contributors" }]
license = { text = "MIT" }

dependencies = [
    "pydantic>=2.7,<3",
    "pyyaml>=6.0.1",
    "docker>=7.1",
    "httpx>=0.27",
    "langchain>=1.3.3,<2",
    "langchain-google-genai>=4,<5",
    "langchain-ollama>=1,<2",
    "langchain-openai>=1,<2",
    "openai>=1.0",
    "google-generativeai>=0.8",
    "ollama>=0.4",
    "keyring>=25.0",
    "langgraph>=1.2.5,<1.3",
    "textual>=0.79",
    "typer>=0.12",
    "rich>=13.0",
]

[project.scripts]
docker-agent = "docker_agent.cli:main"

[dependency-groups]
dev = [
    "pytest>=8.3",
    "pytest-asyncio>=0.23",
    "pytest-httpx>=0.30",
    "ruff>=0.6",
]

[build-system]
requires = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]

[tool.setuptools.packages.find]
where = ["src"]
include = ["docker_agent*"]

[tool.setuptools.package-dir]
"docker_agent" = "src/docker_agent"

[tool.setuptools.package-data]
"docker_agent" = ["prompts/**/*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src", "tests"]
asyncio_mode = "auto"
addopts = "-ra --strict-markers"
filterwarnings = [
    "ignore::DeprecationWarning",
    "ignore::FutureWarning",
]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "C4"]
# E/F = pycodestyle/pyflakes baseline
# I = isort
# UP = pyupgrade
# B = bugbear
# SIM = simplifications
# C4 = comprehensions

[tool.ruff.lint.isort]
known-first-party = ["docker_agent"]
